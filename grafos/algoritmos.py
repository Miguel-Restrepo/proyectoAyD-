from re import X

s=[]
a=[]
u=0
key=None
i=0
t=0
def AlgoritmoQueyranne(grafo):
    return grafo
def PENDENT_PAIR(v,f,x):
    v[0]=x
    for i in len(v)-1:
        update[key]
        v[i+1]=EXTRA_MIN(Q)
    return v[len(v)-1], v[len(v)]
def update(key):
    return key
def EXTRA_MIN(Q):
    return Q
def OPTIMAL_SET(v,f,x):
    for  j in v:
        s[j]={j}
        if s[j] in  j:
            v={s[j]}
    for h in len(v)-1:
        (s[t],s[u])=PENDENT_PAIR(v,f,s[x])
        a[h]=s[i]
        s[h]=s[f]
        s[t]=s[t] & s[u]
        V=V / {s[u]}
    #seleccionar menor de h hasta n-1 si i pertenece a arg
    return a[i]



def AlgoritmoMssf(grafo):
    return grafo    
def AlgoritmoQ_Clustering(grafo):
    return grafo
